const mysql = require("mysql2");

const connectDatabase = () => {
  try {
  } catch (error) {}
  const connection = mysql.createConnection({
    host: "localhost",
    user: "123456",
    database: "hoidanit",
  });
};
